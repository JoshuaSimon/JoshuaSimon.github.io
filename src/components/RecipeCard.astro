---
import RatingStars from "./RatingStars.astro";

interface Props {
    title: string;
    image: string;
    time: number;
    rating: number;
    slug: string;
}

const { title, image, time, rating, slug } = Astro.props;
---

<article
    class="recipe-card bg-white dark:bg-dark-surface rounded-lg shadow-md dark:shadow-gray-900/30 overflow-hidden hover:shadow-xl dark:hover:shadow-gray-900/50 transition-all duration-300 group"
>
    <a
        href={`/recipes/${slug}`}
        class="block focus:outline-none focus:ring-2 focus:ring-amber-500 focus:ring-offset-2 dark:focus:ring-offset-dark-surface rounded-lg"
    >
        <div class="relative overflow-hidden card-shine">
            <img
                src={image}
                alt={title}
                class="w-full h-56 object-cover group-hover:scale-110 transition-transform duration-500 ease-out"
                loading="lazy"
                decoding="async"
            />
            <div
                class="absolute top-3 right-3 bg-white/95 dark:bg-dark-bg/95 backdrop-blur-sm px-3 py-1.5 rounded-full shadow-md dark:shadow-gray-900/30 group-hover:shadow-lg transition-shadow duration-300"
            >
                <span
                    class="text-sm font-medium text-gray-700 dark:text-dark-text flex items-center gap-1"
                >
                    <svg
                        class="w-4 h-4"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                        ></path>
                    </svg>
                    {time} min
                </span>
            </div>
        </div>

        <div class="p-5 sm:p-6">
            <h3
                class="text-xl font-bold text-gray-900 dark:text-dark-text mb-3 group-hover:text-amber-600 transition-colors duration-300 line-clamp-2"
            >
                {title}
            </h3>

            <div class="flex items-center justify-between">
                <RatingStars rating={rating} size="sm" />
                <span
                    class="text-amber-600 font-medium text-sm flex items-center gap-1 opacity-0 group-hover:opacity-100 transition-all duration-300 transform group-hover:translate-x-1"
                >
                    View Recipe
                    <svg
                        class="w-4 h-4"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M9 5l7 7-7 7"></path>
                    </svg>
                </span>
            </div>
        </div>
    </a>
</article>
